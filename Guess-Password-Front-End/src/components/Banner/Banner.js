import React from 'react';
import { Typography } from '@smooth-ui/core-sc';

const Banner = () => (
  <Typography variant="h2" textAlign="center" m={0}>
    GUESS THE PASSWORD
  </Typography>
);

export default Banner;